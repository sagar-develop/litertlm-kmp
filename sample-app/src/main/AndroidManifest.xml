<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Required to download the model on first launch. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- ACCESS_NETWORK_STATE is merged in from :lib's manifest. -->

    <!-- Local peer-to-peer sync (phone <-> tablet on the same Wi-Fi): NSD/mDNS
         discovery needs a multicast lock; the transfer is a plain TCP socket on the
         local network. No internet, no Play Services, no location permission — device
         data moves directly between the user's own devices, never via a server. -->
    <uses-permission android:name="android.permission.CHANGE_WIFI_MULTICAST_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Voice input: record dictated audio for on-device (Whisper) speech-to-text.
         Audio is transcribed locally and never leaves the device. -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <!-- Android 11+ package visibility: bind to an on-device text-to-speech
         engine for Studio read-aloud (no network; speech stays on device). -->
    <queries>
        <intent>
            <action android:name="android.intent.action.TTS_SERVICE" />
        </intent>
    </queries>

    <application
        android:name=".SampleApplication"
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:theme="@style/Theme.NativeLM"
        android:supportsRtl="true"
        android:allowBackup="false"
        android:usesCleartextTraffic="false"
        android:networkSecurityConfig="@xml/network_security_config"
        tools:targetApi="33"
        xmlns:tools="http://schemas.android.com/tools">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.NativeLM.Starting"
            android:configChanges="orientation|screenSize|smallestScreenSize|screenLayout|keyboardHidden|uiMode|fontScale|density">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Zero-telemetry guarantee: ML Kit (on-device OCR) transitively bundles
             Google's datatransport pipeline, which auto-initializes on startup and
             uploads usage/diagnostics to firebaselogging.googleapis.com. Strip the
             CCT backend + its upload schedulers so nothing ever leaves the device.
             On-device OCR does not depend on this telemetry pipeline. -->
        <service
            android:name="com.google.android.datatransport.runtime.backends.TransportBackendDiscovery"
            tools:node="remove" />
        <service
            android:name="com.google.android.datatransport.runtime.scheduling.jobscheduling.JobInfoSchedulerService"
            tools:node="remove" />
        <receiver
            android:name="com.google.android.datatransport.runtime.scheduling.jobscheduling.AlarmManagerSchedulerBroadcastReceiver"
            tools:node="remove" />

    </application>

</manifest>
